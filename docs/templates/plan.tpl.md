---
task: T-000
spec: docs/specs/<F-xx>/          # ถ้ามี
intent: docs/intents/I-000-...md  # ถ้ามี
created: YYYY-MM-DD
approved_by: <ใครอนุมัติ>
---

# Plan: <ชื่องาน>

> เขียนใน **plan mode** (Claude แก้ไฟล์ไม่ได้) แล้ว **commit ก่อนเริ่มเขียนโค้ด**
> เก็บที่ `docs/plans/T-000.md`
>
> **ไฟล์นี้คือตัวบีบอัด** — `/task` และ `/check` จะอ่าน**ไฟล์นี้ไฟล์เดียว** ไม่ย้อนไปอ่าน spec / constitution / architecture ซ้ำ
> อะไรที่ไม่อยู่ในนี้ = ไม่มีใครเห็นตอนลงมือ

## ความเข้าใจของผม
<สรุปว่างานนี้คืออะไร ในภาษาของตัวเอง 3-5 บรรทัด — ถ้าสรุปผิด จะได้แก้ตรงนี้ ไม่ใช่หลังเขียนโค้ดเสร็จ>

**ทำ:**
-

**ไม่ทำในรอบนี้:**
-

## ข้อกำหนดที่คัดมาแล้ว (สิ่งที่ /check ใช้เทียบ — ต้องครบ)

**AC ที่ task นี้ครอบ** (คัดลอกประโยค EARS จาก requirements.md มาตรง ๆ):
- AC-x: WHEN … THE SYSTEM SHALL …

**มาตราธรรมนูญที่ใช้กับงานนี้** (เลข + 1 บรรทัดว่าหมายถึงอะไรในงานนี้ — ปกติ 2-4 มาตรา):
- ม.4 เล็กก่อน → <เช่น ไม่ทำ generic export ทำเฉพาะ CSV ตาม AC-2>
- ม.6 สัญญาก่อน → <เช่น เพิ่ม endpoint ต้อง export openapi.json ใน task นี้>

**กติกาจาก design.md ที่เกี่ยว** (error code, i18n key, component ที่ต้องใช้, schema diff):
-

**pattern เดิมที่ต้องทำตาม** (ไฟล์ตัวอย่างในโปรเจกต์):
- `src/...` — <ทำอะไรเหมือนมัน>

## ไฟล์ที่จะเปลี่ยน
| ไฟล์ | ทำอะไร | ทำไม |
|---|---|---|
| `src/...` | เพิ่ม/แก้/ลบ | |

> เกิน ~10 ไฟล์ = task ใหญ่เกินไป กลับไปแตก task ก่อน

## ลำดับงาน
1.
2.
3.

> เรียงให้แต่ละขั้น **verify ได้ทีละขั้น** อย่าเขียนรวดแล้วค่อยรันตอนท้าย

## ความเสี่ยง
| ความเสี่ยง | จะรู้ได้ยังไงว่าเกิด | แผนรับมือ |
|---|---|---|

## Proof — อะไรพิสูจน์ว่าเสร็จ
> ข้อนี้ห้ามเว้นว่าง ถ้าเขียนไม่ได้แปลว่ายังไม่เข้าใจงาน

- [ ] `{{VERIFY_COMMAND}}` ผ่าน (แปะบรรทัดสรุป)
- [ ] เทสใหม่: `<ชื่อไฟล์เทส>` ครอบ AC-x, AC-y
- [ ] ยิงจริง: `<คำสั่ง/endpoint/หน้าจอ>` แล้วได้ `<ผลลัพธ์ที่คาด>`
- [ ] (ถ้าเป็น UI) ภาพหน้าจอเทียบกับ `<design/หน้าเดิม>` ที่ ~390px และ 1280px, light + dark

## จุดที่อยากให้ตัดสินใจก่อนเริ่ม
- [NEEDS CLARIFICATION: <คำถาม>]

---

## เปลี่ยนแผนกลางทาง
ถ้าระหว่างทำแล้วแผนใช้ไม่ได้: **หยุด กลับมาแก้ไฟล์นี้ บอกผู้ใช้ว่าเปลี่ยนอะไรเพราะอะไร แล้วค่อยไปต่อ**
ห้ามเบี่ยงจากแผนเงียบ ๆ เพราะ `plan.md` กับ diff ที่ไม่ตรงกันคือสัญญาณว่างานหลุด scope

| แก้เมื่อ | เปลี่ยนอะไร | เพราะอะไร |
|---|---|---|
